spring:
  data:
    mongodb:
      auto-index-creation: true

  threads:
    virtual:
      enabled: false

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.14

server:
  port: 0

logging:
  level:
    root: WARN
    com.dbd.service.pagos_tarjetas: DEBUG
